{
  "name": "p2p-web-share-server",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-knowledge WebRTC signaling server for P2P Web Share. Relays handshakes only — never reads, processes, or stores file data.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node test/signaling.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
